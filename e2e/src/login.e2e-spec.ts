import { browser } from "protractor";
import { Login } from "./page-objects/login.po";

describe('Login Page', () => {
    let login: Login;

    beforeEach(() => {
        login = new Login();
        login.navigateToLogin();
    });

    it('', () => {

    });
    it('', () => {

    });
    it('', () => {

    });
    it('', () => {

    });
});